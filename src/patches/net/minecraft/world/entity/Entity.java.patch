--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -538,7 +_,7 @@
         }
         firstTick = false;
         if (level() instanceof ServerLevel serverLevel && this instanceof Leashable) {
-            Leashable.tickLeash(serverLevel, (E) (Entity) (Leashable) this);
+            Leashable.tickLeash(serverLevel, (Entity & Leashable) this);
         }
         profiler.pop();
     }
