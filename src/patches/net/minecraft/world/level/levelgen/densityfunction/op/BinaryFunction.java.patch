--- a/net/minecraft/world/level/levelgen/densityfunction/op/BinaryFunction.java
+++ b/net/minecraft/world/level/levelgen/densityfunction/op/BinaryFunction.java
@@ -57,7 +_,7 @@
     public void fillArray(float[] output, DensityFunction.ContextProvider contextProvider) {
         this.left.fillArray(output, contextProvider);
         switch (this.type) {
-            case ADD:
+            case ADD: {
                 float[] right = new float[output.length];
                 this.right.fillArray(right, contextProvider);
 
@@ -65,7 +_,8 @@
                     output[i] += right[i];
                 }
                 break;
-            case SUB:
+            }
+            case SUB: {
                 float[] right = new float[output.length];
                 this.right.fillArray(right, contextProvider);
 
@@ -73,6 +_,7 @@
                     output[i] -= right[i];
                 }
                 break;
+            }
             case MUL:
                 for (int i = 0; i < output.length; i++) {
                     float left = output[i];
