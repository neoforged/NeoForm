--- a/net/minecraft/commands/arguments/item/ItemInput.java
+++ b/net/minecraft/commands/arguments/item/ItemInput.java
@@ -73,6 +_,6 @@
     }
 
     private String getItemName() {
-        return item.unwrapKey().map(ResourceKey::identifier).orElseGet(() -> "unknown[" + item + "]").toString();
+        return item.unwrapKey().<Object>map(ResourceKey::identifier).orElseGet(() -> "unknown[" + item + "]").toString();
     }
 }
